# Codex Memory Pointer

**Stale (2026-07-16):** the swing agent below was retired to `legacy/` and replaced by
the intraday paper engine — see `CLAUDE.md` and `docs/intraday_operations.md`.

Primary persistent context for swing tuning is stored at:
- `legacy/docs/swing_tuning_notes_2026-04-03.md`

Quick summary:
- Fixed SL/TP internal unit bug (fraction vs percent).
- Added migration `005_normalize_swing_sl_tp_units.py` for historical DB rows.
- Revised swing exit logic to reduce noise exits.
- Added soft-exit loss guard and `CLOSE-CHECK` logs for iterative tuning.
- Current tuned knobs in `app/swing/config.py`:
  - `SHORT_EXIT_RSI_FLOOR = 32.0`
  - `LONG_EXIT_RSI_CEIL = 68.0`
  - `SOFT_EXIT_MAX_LOSS_PCT = 0.020`
